<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Programming Jokes</title>
</head>
<body>
  <h1>Programming Jokes</h1>

  <form action="/joke" method="get">
    <h2>Select the type of joke 👇</h2>
    <button type="submit" name="type" value="single">Single</button>
    <button type="submit" name="type" value="twopart">Two-part</button>
  </form>

  <% if (locals.joke) { %>
    <% if (joke.type === "single") { %>
      <p><%= joke.joke %></p>
    <% } else { %>
      <p><%= joke.setup %><br/><%= joke.delivery %></p>
    <% } %>
  <% } %>
</body>
</html>